name: Build

on:
  push:
    branches:
      - gh-actions
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4
        with:
          path: actions

      - uses: actions/checkout@v4
        with:
          ref: main
          path: chariot

      - name: Download OpenJDK
        run: |
          download_url="https://download.java.net/java/GA/jdk26/c3cc523845074aa0af4f5e1e1ed4151d/35/GPL/openjdk-26_linux-x64_bin.tar.gz"
          wget -O $RUNNER_TEMP/java_package.tar.gz $download_url

      - uses: actions/setup-java@v4
        with:
          distribution: 'jdkfile'
          jdkFile: ${{ runner.temp }}/java_package.tar.gz
          java-version: '26'
          architecture: x64

      - name: Check java version
        run: java -Xinternalversion

      - name: Copy pom template
        run: cp actions/pom.template.xml .

      - name: Fetch tags
        run: cd chariot && git fetch --tags

      - name: Run the build.sh script
        run: actions/build.sh

      - name: checksum
        run: sha256sum bundle/*

      - uses: actions/upload-artifact@v4
        with:
          name: jars
          path: bundle/*
